name: ops_ahead
version: "1.0.0"
profile: ops_ahead

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  ops_ahead:
    staging:
      +materialized: view
    marts:
      +materialized: table
      +engine: "MergeTree()"
