{
  "name": "@ops-ahead/ui-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "HTTP boundary for ns:ui — per-tenant webhooks into events.raw.{alert,monitor}, on-demand analyses into trigger.ml/trigger.data, status in Postgres (ns:ui)",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "node --watch --env-file-if-exists=.env src/server.ts",
    "start": "node dist/server.js",
    "build": "tsc",
    "typecheck": "tsc --noEmit -p tsconfig.test.json",
    "test": "vitest run",
    "test:unit": "vitest run --project unit",
    "test:integration": "vitest run --project integration --passWithNoTests",
    "test:e2e": "vitest run --project e2e",
    "test:watch": "vitest",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "db:migrate": "node --env-file-if-exists=.env node_modules/.bin/prisma migrate deploy",
    "db:generate": "node --env-file-if-exists=.env node_modules/.bin/prisma generate"
  },
  "dependencies": {
    "@fastify/autoload": "6.5.0",
    "@fastify/cors": "11.3.0",
    "@fastify/helmet": "13.1.0",
    "@fastify/sensible": "6.0.4",
    "@fastify/swagger": "9.8.1",
    "@fastify/swagger-ui": "6.1.1",
    "@modelcontextprotocol/fastify": "2.0.0",
    "@modelcontextprotocol/node": "2.0.0",
    "@modelcontextprotocol/server": "2.0.0",
    "@prisma/adapter-pg": "7.10.0",
    "@prisma/client": "7.10.0",
    "close-with-grace": "2.5.0",
    "fastify": "5.11.0",
    "fastify-plugin": "6.0.0",
    "fastify-type-provider-zod": "7.0.0",
    "kafkajs": "2.2.4",
    "pg": "8.23.0",
    "prisma": "7.10.0",
    "prom-client": "15.1.3",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.6",
    "@types/node": "24.13.3",
    "@types/pg": "8.23.1",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
