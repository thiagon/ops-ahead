# Deploy identity for this app (moved out of pyproject's [tool.ops-ahead]).
# Declarative: the base chart renders it; the overlay + version live alongside.
workload: deployment
namespace: ml
chart: infra/charts/ml-model-serving
