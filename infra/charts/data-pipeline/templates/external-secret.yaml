apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ .Values.secretName }}
  namespace: {{ .Release.Namespace }}
  annotations:
    # Created before any Workflow can run against it.
    argocd.argoproj.io/sync-wave: "-1"
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-kv
    kind: ClusterSecretStore
  target:
    name: {{ .Values.secretName }}
    creationPolicy: Owner
  # One Vault path for every secret this app needs — same pattern as
  # ml-mlflow's `mlflow` key (8 properties, one path). Never split an app's
  # own secrets across multiple Vault paths by resource.
  data:
    - secretKey: clickhouse-password
      remoteRef: { key: data-pipeline, property: CLICKHOUSE_PASSWORD }
    - secretKey: minio-access-key
      remoteRef: { key: data-pipeline, property: MINIO_ROOT_USER }
    - secretKey: minio-secret-key
      remoteRef: { key: data-pipeline, property: MINIO_ROOT_PASSWORD }
