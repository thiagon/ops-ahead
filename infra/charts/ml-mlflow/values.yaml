mlflow:
  image: ghcr.io/mlflow/mlflow:v3.12.0
  replicas: 1
  artifactRoot: s3://ops-ahead-lake/mlflow
  minio:
    endpoint: http://minio.data.svc.cluster.local:9000
    accessKey: ""
    secretKey: ""

networkPolicy:
  enabled: true
