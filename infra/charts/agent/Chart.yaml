apiVersion: v2
name: ops-ahead-agent
description: Ops Ahead agent namespace — Postgres + pgvector, LiteLLM proxy
type: application
version: 0.1.0
appVersion: "1.0.0"
dependencies:
  - name: postgresql
    version: "15.5.17"
    repository: "https://charts.bitnami.com/bitnami"
    alias: agent-postgres
    condition: postgres.enabled
