apiVersion: v1
kind: ConfigMap
metadata:
  name: llm-postgres-init
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "llm-postgres.labels" . | nindent 4 }}
data:
  00-init.sql: |
    CREATE EXTENSION IF NOT EXISTS vector;

    CREATE TABLE IF NOT EXISTS agent_calls (
      id          SERIAL PRIMARY KEY,
      created_at  TIMESTAMPTZ DEFAULT NOW(),
      session_id  TEXT,
      tool_name   TEXT,
      input       JSONB,
      output      JSONB,
      duration_ms INTEGER
    );

    CREATE TABLE IF NOT EXISTS recommendations (
      id             SERIAL PRIMARY KEY,
      created_at     TIMESTAMPTZ DEFAULT NOW(),
      incident_id    TEXT,
      recommendation TEXT,
      confidence     FLOAT,
      embedding      vector(1536)
    );

    CREATE TABLE IF NOT EXISTS actions (
      id                SERIAL PRIMARY KEY,
      created_at        TIMESTAMPTZ DEFAULT NOW(),
      recommendation_id INTEGER REFERENCES recommendations(id),
      action_type       TEXT,
      payload           JSONB,
      status            TEXT DEFAULT 'pending'
    );
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: llm-postgres
  namespace: {{ .Release.Namespace }}
  labels:
    app: llm-postgres
    {{- include "llm-postgres.labels" . | nindent 4 }}
spec:
  serviceName: llm-postgres-headless
  replicas: 1
  selector:
    matchLabels:
      app: llm-postgres
  template:
    metadata:
      labels:
        app: llm-postgres
    spec:
      containers:
        - name: postgres
          image: pgvector/pgvector:pg16
          env:
            - name: POSTGRES_USER
              value: {{ index .Values "llm-postgres" "auth" "username" | quote }}
            - name: POSTGRES_DB
              value: {{ index .Values "llm-postgres" "auth" "database" | quote }}
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ index .Values "llm-postgres" "auth" "existingSecret" | quote }}
                  key: {{ index .Values "llm-postgres" "auth" "secretKeys" "userPasswordKey" | quote }}
          ports:
            - containerPort: 5432
              name: postgres
          resources:
            {{- toYaml (index .Values "llm-postgres" "primary" "resources") | nindent 12 }}
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
              subPath: pgdata
            - name: init-scripts
              mountPath: /docker-entrypoint-initdb.d
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", {{ index .Values "llm-postgres" "auth" "username" | quote }}]
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", {{ index .Values "llm-postgres" "auth" "username" | quote }}]
            initialDelaySeconds: 30
            periodSeconds: 10
      volumes:
        - name: init-scripts
          configMap:
            name: llm-postgres-init
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: {{ index .Values "llm-postgres" "primary" "persistence" "size" }}
---
apiVersion: v1
kind: Service
metadata:
  name: llm-postgres
  namespace: {{ .Release.Namespace }}
  labels:
    app: llm-postgres
    {{- include "llm-postgres.labels" . | nindent 4 }}
spec:
  selector:
    app: llm-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: llm-postgres-headless
  namespace: {{ .Release.Namespace }}
  labels:
    app: llm-postgres
    {{- include "llm-postgres.labels" . | nindent 4 }}
spec:
  clusterIP: None
  selector:
    app: llm-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
