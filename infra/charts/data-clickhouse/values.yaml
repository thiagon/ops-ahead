clickhouse:
  operatorEnabled: true
  cluster:
    shards: 1
    replicas: 2
    # Must stay under the container memory limit (2Gi). 1.5 GiB leaves headroom.
    maxServerMemoryUsage: "1610612736"
    storage:
      size: 50Gi

monitoring:
  enabled: true

altinity-clickhouse-operator: {}
