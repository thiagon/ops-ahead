apiVersion: v2
name: ml-burst-detector
description: Stateless burst detector — adaptive z-score + CUSUM over events.monitor, Redis-backed state (ns:ml)
type: application
version: 0.1.0
appVersion: "0.1.0"
