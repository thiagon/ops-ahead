apiVersion: v2
name: data-runner
description: data-runner's own workload — Deployment scaled by a KEDA ScaledObject consuming trigger.data (ns:data)
type: application
version: 0.1.0
appVersion: "0.1.0"
