{{- if .Values.cron.enabled }}
# Native CronJob — asks the gateway to start one full_pipeline and exits.
# It goes through POST /analyses like any other client instead of publishing
# to Kafka directly, so the daily run gets a row, a status and a run_key:
# no execution exists that GET /analyses cannot answer for. The scheduler API
# key is what marks the run as the clock's — provenance is the credential's to
# declare, never the body's. The id is minted by the gateway and printed here.
apiVersion: batch/v1
kind: CronJob
metadata:
  name: data-runner-daily
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "data-runner.labels" . | nindent 4 }}
spec:
  schedule: {{ .Values.cron.schedule | quote }}
  timeZone: "UTC"
  concurrencyPolicy: Forbid
  startingDeadlineSeconds: 300
  jobTemplate:
    spec:
      backoffLimit: 2
      # Must outlast curl's own retry window below, or the deadline kills
      # the attempt the retries exist to make.
      activeDeadlineSeconds: 1200
      template:
        metadata:
          labels:
            app: data-runner-daily
            {{- include "data-runner.labels" . | nindent 12 }}
        spec:
          restartPolicy: Never
          automountServiceAccountToken: false
          securityContext:
            seccompProfile:
              type: RuntimeDefault
          containers:
            - name: publish
              image: {{ .Values.cron.image | quote }}
              securityContext:
                allowPrivilegeEscalation: false
                capabilities:
                  drop: [ALL]
              command: ["sh", "-c"]
              args:
                - |
                  set -eu
                  # --retry-connrefused is the point: a refused connection is
                  # what a gateway rollout looks like from here, and plain
                  # --retry treats it as final. This fires once a day, so an
                  # attempt that gives up costs a whole run of the chain.
                  curl -sS --fail-with-body -X POST "$GATEWAY_URL/analyses" \
                    --retry 6 --retry-delay 10 --retry-connrefused \
                    --retry-max-time 300 --max-time 30 \
                    -H 'Content-Type: application/json' \
                    -H "Authorization: Bearer $SCHEDULER_API_KEY" \
                    -d '{"analysis":"full_pipeline"}'
              env:
                - name: GATEWAY_URL
                  value: {{ .Values.run.env.gatewayUrl | quote }}
                - name: SCHEDULER_API_KEY
                  valueFrom:
                    secretKeyRef:
                      name: {{ .Values.run.secretName }}
                      key: scheduler-api-key
{{- end }}
