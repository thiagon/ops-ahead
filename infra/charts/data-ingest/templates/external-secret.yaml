apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ .Values.consumer.secretName }}
  namespace: {{ .Release.Namespace }}
  annotations:
    # Created before the Deployment (wave 0) so the consumer can mount it.
    argocd.argoproj.io/sync-wave: "-1"
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-kv
    kind: ClusterSecretStore
  target:
    name: {{ .Values.consumer.secretName }}
    creationPolicy: Owner
  data:
    - secretKey: CLICKHOUSE_PASSWORD
      remoteRef: { key: clickhouse, property: CLICKHOUSE_PASSWORD }
    - secretKey: minio-access-key
      remoteRef: { key: minio, property: MINIO_ROOT_USER }
    - secretKey: minio-secret-key
      remoteRef: { key: minio, property: MINIO_ROOT_PASSWORD }
