# Never for the app image (built locally, imported into k3d via `make build`).
image:
  pullPolicy: Never

# migrate/migrate is a public image — it must be pulled, not Never.
migration:
  pullPolicy: IfNotPresent

consumer:
  replicaCount: 1
  env:
    batchMaxSize: "100"
    batchMaxSeconds: "5"
