apiVersion: v2
name: infra-gitea
description: Gitea — local Git mirror (dev only)
type: application
version: 0.1.0
appVersion: "1.22.0"
dependencies:
  - name: gitea
    version: "10.4.1"
    repository: "https://dl.gitea.io/charts/"
