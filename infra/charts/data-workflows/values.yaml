argo-workflows:
  controller:
    replicas: 1
  server:
    replicas: 1
    authModes:
      - server

# Namespace-scoped executor SA + Role, shared by every pipeline that runs on this
# engine (pipelines/*). Enabled here since this chart is now engine-only.
argoWorkflows:
  rbac:
    enabled: true
