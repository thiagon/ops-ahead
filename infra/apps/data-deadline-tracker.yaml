apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: data-deadline-tracker
  namespace: infra
  annotations:
    # After data-kafka (6, the topic it consumes/publishes) and
    # data-clickhouse (5, silver_alert_open/tenant_deadlines it reads) —
    # same tier as data-ingest.
    argocd.argoproj.io/sync-wave: "7"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  revisionHistoryLimit: 3
  project: ops-ahead
  sources:
    - repoURL: https://github.com/thiagon/ops-ahead
      targetRevision: main
      ref: values
    - repoURL: https://github.com/thiagon/ops-ahead
      targetRevision: main
      path: infra/charts/data-deadline-tracker
      helm:
        valueFiles:
          - values.yaml
          - $values/apps/data-deadline-tracker/chart/values-dev.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: data
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - SkipDryRunOnMissingResource=true
    retry:
      limit: 5
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 3m
