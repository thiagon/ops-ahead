# Every containerd-shim and the k3s server hold inotify instances under root's
# UID; Ubuntu's default of 128 runs out at ~50 pods.
fs.inotify.max_user_instances = 1024
fs.inotify.max_user_watches = 524288
